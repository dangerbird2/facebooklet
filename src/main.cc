//
// Created by Steven on 4/18/15.
//

#include "face.h"
#include <sstream>
#include <stdio.h>

using namespace std;
using namespace fb;

int main(int argc, char **argv)
{
  int m, d, y;
  m = d = y = -1;
  cin >> m >> d >> y;
  printf("%d %d %d\n", m, d, y);
  //FaceBooklet f(Database(), cin);
  //f.run();

  return 0;
}